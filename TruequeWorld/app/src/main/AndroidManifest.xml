<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/elegido"
        android:label="@string/app_name"
        android:roundIcon="@drawable/elegido"
        android:supportsRtl="true"
        android:theme="@style/Theme.TruequeWorld"
        android:usesCleartextTraffic="true"
        tools:ignore="ExtraText"
        tools:targetApi="31" >
        <activity
            android:name=".UpdateUser"
            android:exported="false" />
        <activity
            android:name=".Addproductscreen"
            android:exported="false" />
        <activity
            android:name=".StartScreen"
            android:exported="false"
            android:noHistory="true" />
        <activity
            android:name=".SplashScreen"
            android:exported="true"
            android:noHistory="true" >
            " /**EVITAR QUE EL USUARIO PUEDA RETROCEDER AL LAYOUT ANTERIOR**/"
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainScreen"
            android:exported="false" />
        <activity
            android:name=".Preference_screen"
            android:exported="false" />
        <service
            android:name=".firebase.MessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

    </application>

</manifest>